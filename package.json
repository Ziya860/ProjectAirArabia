{
  "name": "cypressautomation",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Ziyad",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^14.0.1"
  },
  "scripts": {
    "test": "cypress run",
    "merge-reports": "mochawesome-merge cypress/reports/*.json > cypress/reports/report.json",
    "generate-report": "marge cypress/reports/report.json --reportDir cypress/reports --inline",
    "posttest": "npm run merge-reports && npm run generate-report && node send-report.js"
  }
}

"devDependencies": {
"cypress": "^14.0.1",
"cypress-mochawesome-reporter": "^3.8.0"
}
